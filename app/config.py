# 검출 기본 파라미터 (요청으로 override 가능)
# 목표: 재현율 우선 — 흐린/작은 콜로니까지 최대한 검출.
DEFAULT_MIN_AREA = 6.0         # 작은 콜로니 재현율↑ (튜닝: 8→6)
DEFAULT_MAX_AREA = 5000.0
DEFAULT_MIN_CIRCULARITY = 0.42 # 약간 찌그러진 콜로니도 통과 (튜닝: 0.45→0.42)
DEFAULT_INVERT = True          # 콜로니가 배경보다 어두우면 True (black top-hat)
DEFAULT_TOPHAT_KERNEL = 31     # top-hat 구조요소 크기(px). 콜로니보다 크게 잡아야 함
DEFAULT_MASK_WALLS = True      # 밝은 plate 내부(agar) ROI로 제한 → 프레임/벽/배경 제외

# 민감도 knob — Otsu 전역 임계값에서 이만큼 뺀다.
DEFAULT_THRESHOLD_OFFSET = 7   # >0 → 임계값↓ → 더 민감(흐린 것↑, 노이즈↑). <0 → 더 엄격.
                               # 유효 sweet spot 대략 -4 ~ +10 (그 밖은 뭉침/노이즈).
                               # 기본 +7: plate를 반듯하게 꽉 채워 찍는 조건에서 흐린 콜로니
                               # 재현율을 최대화한 값(샘플 튜닝). +8↑부터 노이즈 클러스터
                               # 발생, +10↑에서 임계값 바닥 닿아 질감까지 검출됨.

# 벽면 메니스커스(agar가 벽에 붙어 생기는 길고 얇은 어두운 주름) 제거.
# black-hat이 이 선형 그림자를 콜로니로 오검출 → watershed가 여러 조각으로 쪼갬.
# "길고(최대변≥LEN) 얇은(aspect≥ASPECT)" 연결성분만 제거. compact한 콜로니/클러스터는 보존.
MENISCUS_MIN_LEN = 80          # 실측: 메니스커스 최대변 170~326 vs 클러스터 ≤167
MENISCUS_MIN_ASPECT = 4.0      # 실측: 메니스커스 aspect 8~19 vs 클러스터 ≤2.8 (큰 간극)

# plate ROI 마스크 (밝은 agar 영역만 남김)
ROI_CLOSE_KERNEL = 35          # 콜로니 구멍을 메워 plate를 하나의 덩어리로
ROI_ERODE_KERNEL = 45          # 프레임/벽 경계에서 안쪽으로 크게 수축해 벽 오검출 방지
                               # (튜닝: 15→45. _well_mask에서 격자∩이 ROI로 바깥벽 제외)

# 8개 웰(덱) 격자 — plate를 규칙 격자로 나눠 웰 내부만 남기고 벽/프레임 제외.
# 하드웨어는 2행 × 4열 = 8덱(A1~B4). mask_walls=True일 때 이 격자로 검출을 제한.
WELL_ROWS = 2
WELL_COLS = 4
WELL_MARGIN = 40               # 각 웰 셀을 안쪽으로 줄이는 여백(px) — 벽/프레임 배제

# 붙은 콜로니 분리(watershed) — 기본 ON. 밀집 구간 재현율↑ (뭉친 콜로니 개별화).
# 가장자리 오검출이 조금 늘 수 있어, 필요 시 요청에서 split_touching:false로 끌 수 있음.
DEFAULT_SPLIT_TOUCHING = True
WATERSHED_MIN_DISTANCE = 5     # 씨앗(국소 최대) 간 최소 간격(px)
WATERSHED_SEED_MIN = 2.0       # 씨앗으로 인정할 최소 거리변환 값(px)

# 피킹 대상 선별(scoring) — 기본적으로 **필터를 끈다**: 검출된 것 = 피킹 대상.
#
# 이 기준들은 8웰 플레이트(1350×910)에 맞춰 원본 픽셀 단위로 튜닝된 값이었다.
# 4000px 페트리 이미지에서는 사실상 아무것도 걸러내지 못했다 — 실측상 검출
# 1,333개 중 1,286개(96.5%)가 통과했고, 탈락 47개도 대부분 테두리였다.
# 원형도는 검출 게이트(0.70)가 이미 더 엄격해서 **논리적으로 0개** 탈락이었다.
#
# 즉 구분이 있는 척하면서 실제로는 없었다. 그래서 껐다 — 검출된 것을 모두
# 피킹 대상으로 본다. 응답의 pickable/score/pick_top_n 은 유지되므로,
# score 로 랭킹하거나 pick_top_n 으로 상한을 두는 것은 그대로 동작한다.
#
# 다시 켜야 하는 경우: 붙은 콜로니를 집으면 두 균주가 섞인 혼합 클론이 되고,
# 그건 며칠 뒤 시퀀싱 단계에서야 드러난다. 클론 순도가 중요해지면 아래 값을
# 올리되, **원본 픽셀이 아니라 접시 지름 대비 비율로** 다시 설계해야 한다
# (그러지 않으면 카메라·해상도가 바뀔 때 조용히 무력화된다 — 지금이 그 사례).
PICK_MIN_SEPARATION = 0.0      # 이웃 중심과의 최소 거리(px). 0 = 제한 없음.
PICK_ISOLATION_REF = 50.0      # 고립 점수 만점 기준 거리(px) — 랭킹에만 쓰인다
PICK_RADIUS_MIN = 0.0          # 최소 반지름(px). 0 = 제한 없음.
PICK_RADIUS_MAX = 0.0          # 최대 반지름(px). 0 = 제한 없음.
                               # 단위가 원본 픽셀이라 해상도에 의존한다 — 상한을
                               # 두면 콜로니가 큰 접시에서 피킹 대상이 전부
                               # 사라진다(실측: 13895.jpg 검출 15 / 피킹 0).
PICK_MIN_CIRCULARITY = 0.0     # 최소 원형도. 0 = 제한 없음.
PICK_W_ISOLATION = 0.7         # 점수 가중치: 고립도 (랭킹용)
PICK_W_SIZE = 0.3              # 점수 가중치: 크기 적합도 (랭킹용)
PICK_EDGE_MARGIN = 0           # 경계에서 안쪽으로 줄일 여백(px). 0 = 제한 없음.
                               # 요청의 edge_margin(0~100)으로 켤 수 있다.

# ---------------------------------------------------------------------------
# blob 검출기 (app/blob_detector.py) — 기본 검출 경로.
# `scripts/evaluate_labeled.py sample` 로 POST /detect 를 거쳐 측정한 값
# (라벨 40장, 정답 1,886개): 정밀도 90.9% / 재현율 49.6%.
# 같은 샘플에서 위 top-hat 경로는 정밀도 1.2% / 재현율 23.8%였고 네 그룹 모두
# 무작위 산포보다 나빴다(극성·스케일·ROI 가정이 실제 이미지와 불일치).
# 자세한 원인과 판정 축은 app/blob_detector.py 모듈 docstring 참조.
# ---------------------------------------------------------------------------
BLOB_WORK_SIZE = 1280          # 처리 해상도(최대변).
                               # 1024 에서 올렸다 — **곡선 전체가 위로 밀린다.**
                               # 같은 정밀도에서 재현율(39장 실측):
                               #   ~90.2% → 62.1% → **63.3%**  (+1.2%p)
                               #   ~83.3% → 66.3% → **67.6%**  (+1.3%p)
                               #   ~76.6% → 68.9% → **70.5%**  (+1.6%p)
                               # 그룹별로 bright 66→69%, vague 32→38%,
                               # lower-res 77→79% 가 오르고 dark 만 81→80%.
                               #
                               # 1536 은 여전히 나쁘다(정밀도 70.8%) — 1280 이
                               # 좁은 최적점이다. 보고서가 1536 을 기각하며 지목한
                               # 교란("크기 창이 함께 작아진다")은 지금 코드가
                               # _k = work_size / BLOB_WORK_SIZE 로 r_min/r_max 를
                               # 비례 조정해 이미 통제돼 있다.
                               #
                               # 대가는 비용이다. 1280² 은 1024² 대비 픽셀이
                               # 1.56배라 처리시간이 그만큼 늘어난다.
BLOB_MIN_T = 25.0              # 면적 가중 t-통계량 하한 — 이 값이 곧 "감도"다.
                               # 엔드포인트 실측 곡선 (39장 · 극성 자동 판정 +
                               # solidity 0.75 반영 후, 정밀도/재현율):
                               #   t=35 → 93.8%/59.5%   t=25 → 86.4%/65.2%
                               #   t=20 → 79.9%/67.8%   t=15 → 70.5%/71.4%
                               #   t=12 → 62.9%/73.5%
                               #
                               # **35 에서 25 로 내렸다.** 접시별 극성 자동 판정이
                               # 정밀도를 5.3%p 벌어줬고, 이 프로젝트 용도는 피킹
                               # 대상 확보이므로 그 여유를 재현율로 바꾸는 것이
                               # 맞다는 판단이다. 세션 시작(91.4%/57.6%) 대비
                               # 재현율 +7.6%p, 정밀도 −5.0%p.
                               #
                               # 대가: 100개 집으면 빈 한천이 약 9개에서 14개로
                               # 늘어난다. 오검출 비용이 더 크다면(시퀀싱까지
                               # 가는 워크플로) 35 로 되돌리는 것이 맞다.
                               #
                               # 바꿀 때는 **param_mapping.sensitivity_to_min_t 의
                               # 앵커와 반드시 함께** 움직여야 한다. 어긋나면
                               # sensitivity 를 보내는 클라이언트와 보내지 않는
                               # 클라이언트가 다른 감도로 동작하고, 그 차이는
                               # 눈으로 알아채기 어렵다.
BLOB_MIN_REL_SAT = 1.5         # |내부채도 - 주변채도| 하한. 점자식 인쇄 잉크·데브리는
                               # 이 값이 0 근처(-0.1~+1.7), 콜로니는 7~57.
                               # 반드시 절댓값 — 콜로니가 한천보다 채도가 낮은
                               # 접시도 있어(실측 -9.1), 부호를 쓰면 전멸한다.
BLOB_ADAPTIVE_SCALE = False    # 기본 끔 — 실측에서 오히려 나빴다.
                               # 그룹별 최적 해상도는 실제로 다르다:
                               #   bright  1024 F1 64.6 → 1536 F1 68.0
                               #   vague   1024 F1 26.1 → 1536 F1 29.5
                               #   lower   1024 F1 77.6 → 1536 F1 56.8
                               #   dark    1024 F1 80.7 → 1536 F1 71.2
                               # 그래서 자동 선택을 시도했으나 전체 F1이
                               # 65.7 → 60.6(목표 14px) / 59.2(16px)로 떨어졌다.
                               # 원인: 1차 검출의 반지름은 국소 임계값 윤곽
                               # 기준이라 흐린 콜로니의 밝은 심부만 잡아 실제보다
                               # 작게 나온다 → 크기를 과소평가해 해상도를 과하게
                               # 올린다. 크기 추정을 개선하지 않는 한 쓸 수 없다.
                               # 해상도를 조절하려면 work_size를 직접 지정할 것.
BLOB_SCALE_TARGET_DIAM = 16.0  # 목표 콜로니 지름(작업 픽셀). bright 그룹이
                               # 1024에서 10.8px, 1536에서 16px이었다.
BLOB_SCALE_MAX_UPSCALE = 1.5   # 해상도 상향 배율 상한. 2048(2배)은 전 그룹에서
                               # 나빴다(정밀도 43.0%) — 1536이 상한이다.
BLOB_SCALE_PROBE_MIN = 5       # 1차 검출이 이보다 적으면 크기 추정을 신뢰하지 않고
                               # 그대로 반환한다
BLOB_LIMIT_CONTOUR = 0.0       # 윤곽 추출을 후보 주변 반지름 r*이 값 원반으로 제한.
                               # **기각 — 기본 끔.** 붙은 콜로니를 개별 판정하려는
                               # 시도였다(놓침의 31%가 겹치는 라벨 영역).
                               # 실측 F1: 끔 65.8 / 무조건 자르기 최고 66.7(정밀도
                               # 90.1→75.0%) / 조건부 최고 65.0. 어느 조합도 기준선을
                               # 넘지 못했다. 원반으로 자르면 모든 후보가 둥근 윤곽을
                               # 갖게 되어 모양 게이트가 무력화되고, 글씨 덩어리·
                               # 테두리·한천 텍스처 위 노이즈까지 통과한다.
                               # 밀집 분리를 하려면 거리변환 watershed 처럼 실제
                               # 경계를 찾아야 한다 — 원반 클리핑은 막다른 길이다.
BLOB_WATERSHED_SPLIT = True    # 붙은 콜로니를 거리변환 watershed 로 분리한다.
                               # **오늘 유일하게 교환 없이 개선된 축이다.**
                               # 실측 (split_area_ratio=2.0):
                               #   정밀도 90.1→90.0% (유지), 재현율 51.9→53.9%
                               #   F1 65.8→67.4, 그리고 **네 그룹 전부 개선**
                               #   (lower 78.0→79.7, bright 64.7→66.8,
                               #    dark 80.7→81.4, vague 26.1→26.9)
                               # 1.5배로 낮추면 재현율 54.8%까지 가지만 정밀도가
                               # 88.4%로 내려간다 — 검출된 것이 전부 피킹 대상이므로
                               # 정밀도를 지키는 2.0을 택했다.
                               #
                               # 원반 클리핑(BLOB_LIMIT_CONTOUR)과 달리 원을 강제로
                               # 씌우지 않아 잘린 조각이 자기 모양을 유지하고 모양
                               # 게이트가 계속 작동한다. 그것이 성패를 갈랐다.
                               #
                               # 시각 확인이 결정적이었다: 밀집 접시에서 감도를
                               # 50→75로 올려도 검출이 248→252로 거의 변하지 않았다.
                               # 대비 문제가 아니라 분할 문제라는 뜻이다.
BLOB_SPLIT_AREA_RATIO = 1.5    # 자연 윤곽 면적이 후보 기대 면적(π r²)의 이 배를
                               # **1.2 에서 올렸다 (2026-08-04).** 반지름 보정으로
                               # r 이 1.3배 커져 기대 면적이 1.69배가 됐고, 같은
                               # 배수가 실질적으로 훨씬 관대해져 분리를 덜 하게
                               # 됐다 — 한 상수를 옮기면 그것에 의존하는 상수의
                               # 보정이 함께 무효가 된다는 사례가 또 하나다.
                               # 곡선 비교(1.2 대 1.5, 같은 정밀도 기준 재현율):
                               #   ~92.6% → +0.4%p   ~88.4% → +0.9%p
                               #   ~81.2% → +0.2%p
                               # 세 지점 모두 위이고 F1 76.9 → 77.2.
                               # (아래 옛 주석의 실측은 반지름 보정 이전 기준이다.)
                               # 넘으면 "병합된 덩어리"로 보고 watershed 분리를 적용.
                               # 낮출수록 적극적으로 나눈다.
                               # 둥글기 게이트(BLOB_MIN_ROUNDNESS) 도입 **전에는**
                               # 1.2에서 정밀도가 87.8%로 떨어져 2.0을 썼다. 둥글기가
                               # 모양을 지켜주게 된 뒤 실측:
                               #   2.0 → 정밀도 92.9% / 재현율 55.4% (F1 69.4)
                               #   1.5 → 91.7% / 56.6% (F1 70.0)
                               #   1.2 → 91.4% / 57.6% (F1 70.7)  ← 채택
                               # 같은 1.2배가 둥글기 도입 후 정밀도 +3.6%p,
                               # 재현율 +1.9%p 로 개선됐다.
BLOB_INK_SIGMA = 0.0           # 패치 중앙값에서 MAD의 이 배 이상 어두운 픽셀을
                               # 인쇄 잉크로 보고 통계·이진화에서 제외한다.
                               # **기각 — 기본 끔.** 실측 F1: 끔 65.8, 2σ 65.9,
                               # 3σ 65.5, 4.5σ 65.8, 6σ 65.5 로 중립이고
                               # vague는 26.1→24.3으로 나빠졌다.
                               # 애초 동기였던 "놓침의 35%가 인쇄 글씨"는 신뢰할 수
                               # 없는 대리 지표였다 — "패치의 2% 이상이 중앙값보다
                               # 훨씬 어두움"으로 분류했는데 그 조건은 테두리·데브리·
                               # 그림자, 그리고 어두운 콜로니 그룹에서는 콜로니
                               # 자체에도 걸린다. 글씨를 완전히 지워도 재현율이 거의
                               # 오르지 않는 것이 그 증거다.
BLOB_COLOUR_CREDIT_MAX = 1.0   # 색 신호가 t 요구치를 낮춰주는 최대 배율.
                               # 1.0 = 끔(색을 AND 게이트처럼 씀). 예: 3.0이면
                               # 색거리가 min_rel_sat의 3배 이상일 때 t 요구치가
                               # 1/3까지 내려간다. 색이 뚜렷한 콜로니를 저대비에서
                               # 살리기 위한 축 — AND 사슬의 곱셈 손실을 피한다.
BLOB_NOISE_FLOOR = 0.5         # 분산 하한(gray level). 완전히 균일한 영역은 분산이
                               # 0이라 t가 폭발해 구조 없는 곳에서 거대 blob이
                               # 검출된다. 8비트 양자화 노이즈 ±0.5 LSB를 하한으로.
BLOB_MONO_SAT_STD = 2.0        # 접시 내부 채도 표준편차가 이보다 작으면 무채색
                               # 이미지로 보고 색 게이트를 끈다(흑백 카메라 대응).
BLOB_MIN_CIRCULARITY = 0.50    # 둘레 기반 원형도 4πA/P². 경계 거칠기에 극도로
                               # 민감해서 0.70에서는 방사형 균사를 가진 진짜 콜로니를
                               # 버렸다. 주된 모양 판정은 아래 둥글기로 넘겼다.
                               # **이 데이터에서는 이제 무력하다** — 0.50에서 0.00까지
                               # 결과가 완전히 동일했다(91.4%/57.7%). 귀속 계측이
                               # 이 게이트 탈락으로 지목한 174개는 같은 콜로니의
                               # 중복 후보였고 실제 손실이 아니었다. 보지 못한
                               # 데이터에서 얇은 획을 거르는 값싼 안전망으로만 남긴다.
BLOB_MIN_SOLIDITY = 0.75       # 오목한 얼룩·긁힘 배제 (면적 ÷ convex hull 면적).
                               # 0.90 에서 내렸다 — **Pareto 곡선 전체가 올라간다.**
                               # 같은 정밀도에서 재현율을 비교한 실측(39장):
                               #   정밀도 ~88% → 58.8% → **60.3%**  (+1.5%p)
                               #   정밀도 ~80% → 62.9% → **64.4%**  (+1.5%p)
                               #   정밀도 ~72% → 65.2% → **67.3%**  (+2.1%p)
                               #   정밀도 ~61% → 68.1% → **70.5%**  (+2.4%p)
                               # 한 지점이 아니라 모든 운영점에서 위이므로 교환이
                               # 아니라 게이트가 잘못 조여져 있던 것이다.
                               # 기본 운영점 기준: 91.4%/57.6% → 88.6%/60.3%,
                               # F1 70.7 → 71.7, 네 그룹 전부 개선
                               # (bright 57→61, dark 76→78, lower 75→77, vague 19→21).
                               # 0.75 아래로는 결과가 완전히 동일해 그것이 포화점이다.
                               #
                               # 같이 시도했다가 **기각한 것: 둥글기 완화.**
                               # 둥글기 0.35 를 얹으면 55.8%/70.3% 인데, 둥글기를
                               # 그대로 두고 min_t 만 15 로 내리면 53.7%/**72.9%** 다.
                               # 같은 정밀도에서 감도가 더 많이 맞히므로 둥글기를
                               # 푸는 것은 손해다. BLOB_MIN_ROUNDNESS 는 0.55 유지.
BLOB_MAX_ASPECT = 2.0          # 선형 성분(메니스커스) 배제
BLOB_MIN_FILL = 0.45           # bounding box 채움율
BLOB_MIN_ROUNDNESS = 0.55      # 면적 ÷ 최소외접원 면적. **주된 모양 판정.**
                               # 둘레 기반 원형도와 달리 경계 거칠기에 둔감하면서
                               # 얇고 긴 획(글씨)은 여전히 낮게 나온다 — 외접원 대비
                               # 면적이 작기 때문이다.
                               # 실측: 원형도 0.70 단독 → 정밀도 90.0%/재현율 53.9%,
                               # 원형도 0.50 + 둥글기 0.55 → **92.9%/55.4%**
                               # (정밀도와 재현율이 동시에 오르고 네 그룹 전부 개선:
                               #  lower 79.7→82.8, bright 66.8→68.7,
                               #  dark 81.4→82.3, vague 26.9→29.2)
# 접시별 극성 자동 판정. True 면 polarity="auto" 가 **접시마다 한쪽을 골라** 쓴다.
# False 면 구동작(양극성 모두 검출 후 병합).
#
# 극성은 데이터셋마다 반대라 전역 상수로 고정할 수 없다. 그래서 기존 구현은
# 양극성을 모두 검출해 병합했는데, 틀린 극성 분기가 기여 없이 오검출만 추가했다.
# 접시 단위로 한쪽을 고르면 그 낭비가 사라진다.
#
# 실측(39장) — 같은 정밀도에서 재현율:
#   ~88.5% → 60.3% → **63.6%**  (+3.3%p)
#   ~75.6% → 66.4% → **69.5%**  (+3.1%p)
#   ~65.3% → 69.0% → **72.8%**  (+3.8%p)
# 모든 운영점에서 위이므로 교환이 아니다. F1 도 71.7 → 74.3 (min_t 25).
#
# 추정 정확도는 **39/39(100%)**. 정규화 대비 부호가 한 장도 틀리지 않았다
# (bright −1.5~−13.7, dark −2.5~−22.0, vague −1.5~−2.4, lower-res +0.95~+3.7).
# 보고서 §8.4c 는 vague 를 "한 장 안에 극성 혼재"라며 판정 불가로 봤지만,
# 개별 콜로니가 섞여도 **접시 수준 집계는 일관**된다.
# 추정 정확도 **43/43** (실제 39장 + 합성 4종). 방법과 기각한 대안은
# blob_detector.estimate_bright 의 docstring 참조.
BLOB_AUTO_POLARITY = True
BLOB_POLARITY_MIN_MARGIN = 0.05  # |위쪽질량 − 아래쪽질량| ÷ 총합 이 이보다 작으면
                                 # 판정을 포기하고 양극성으로 되돌린다.
                                 # 실측 최소 마진이 0.166 이라 0.05 는 충분히
                                 # 아래이면서 진짜 애매한 이미지를 걸러낸다.

# 모양 판정을 몇 개의 이진화 레벨에서 시도할지. 1 = 기존 동작((mi+mo)/2 하나).
#
# 모양 게이트가 판정하는 윤곽은 임계값 하나로 뽑은 것이라, 그 값이 어긋나면
# 진짜 콜로니가 이상한 모양으로 측정돼 탈락한다. 실측(971.jpg): t 문턱을 다
# 열어도 모양 게이트에서 21개(16%)가 죽었고, 그것이 남은 최대 손실이었다.
# 여러 레벨을 훑어 **하나라도 통과하면** 인정하면, 진짜 콜로니는 어느 레벨에선
# 둥글게 잡히고 획 글씨·긁힘은 어느 레벨에서도 둥글지 않다.
#
# OpenCFU(PLOS ONE 2013)의 score-map — 여러 threshold 에서 원형 영역을 누적 —
# 과 같은 발상이지만 적용 단계가 다르다. 후보 생성에는 쓰지 않는다: 계측상
# 원시 LoG 후보가 이미 정답의 93.1%를 덮고 있어 후보를 더 만들 이유가 없다.
BLOB_CONTOUR_LEVELS = 1
BLOB_CONTOUR_SPAN = 0.4        # 레벨을 mo~mi 사이 0.5±span/2 구간에 균등 배치.
                               # 0.4면 0.3~0.7 — 중심·배경 중앙값 사이를 넓게
                               # 훑되 양 극단(순수 배경/순수 내부)은 피한다.
# 반지름 추정 방식. "contour" = 윤곽 면적(기존), "max" = 윤곽과 LoG 스케일의 최대값.
#
# 정답 지름과 비교한 실측(TP 1,707개): 윤곽 중앙 0.73배, LoG 중앙 0.71배로 둘 다
# 계통 과소추정이고 어느 한쪽이 낫지 않다(오차 중앙값 0.29 대 0.30). 독립적인 두
# 추정이 같은 값에 수렴하므로 이 0.7배는 **라벨 관행**(손으로 그린 박스가 콜로니
# 보다 크다)일 가능성이 크다 — 비율만으로는 못 가린다.
#
# 문제는 꼬리다. 하위 10% 지점에서 윤곽 0.30, LoG 0.15 로 파국적으로 작아진다.
# 콜로니를 실제의 6~15% 크기로 재는 경우가 있고, 그것이 (a) 큰 콜로니에 점만 한
# 마커, (b) NMS 가 중복을 못 걸러내는 원인이다 — NMS 억제 반경이
# max(r) * NMS_FRAC 이라 r 이 무너지면 억제도 무너진다.
# t-통계량 표본 영역 (후보 반지름 r 배수).
#
# **이번 세션 최대 단일 개선이다.** 도입 이후 한 번도 튜닝된 적 없는 축이었는데,
# 같은 정밀도 기준 재현율이 **+3.6%p** 올랐다 — 극성 자동 판정(+3.1~3.8%p)과
# 맞먹는다. 그리고 교환이 아니다: 기본 운영점에서 정밀도 83.3 → **87.1%**,
# 재현율 67.6 → **68.9%** 로 양쪽이 함께 오른다.
#
# 왜 되는가. t = (mi−mo) / √(s_in²/n_in + s_out²/n_out) 이다.
#   inner 0.65 → 0.80  내부 픽셀이 늘어 분자(중앙값 추정)가 안정된다.
#   outer 2.1 → 2.8    배경 표본이 늘어 분모가 줄어든다.
# 양쪽에서 신호 대 잡음비가 좋아지므로 감도를 바꾸는 것과 성격이 다르다.
#
# 고리를 넓히면 조명 그라데이션·이웃 콜로니가 섞일까 우려했으나 그렇지 않았다 —
# 배경 추정에 중앙값/MAD 를 쓰기 때문이다. 평균이었다면 독이 됐을 변경이다.
#
# 곡선 검증 (outer 2.8, 기준선 대비):
#   min_t 35 → 90.18%/63.3% 대 89.86%/**65.7%**
#   min_t 25 → 83.32%/67.6% 대 84.96%/**69.2%**
#   min_t 20 → 76.64%/70.5% 대 **79.64%**/**72.2%**
# 세 지점 전부 우세하므로 한 지점의 우연이 아니다.
#
# outer_hi 는 2.8~3.2 에서 포화한다(3.6·4.2 도 같은 성적). 안전하게 낮은 쪽인
# 2.8 을 택했다 — 고리가 클수록 접시 테두리 밖으로 나갈 여지가 커진다.
BLOB_INNER_FRAC = 0.80
BLOB_OUTER_LO = 1.4
BLOB_OUTER_HI = 2.8

BLOB_RADIUS_MODE = "max"       # 윤곽과 LoG 스케일의 최대값. 꼬리가 좋아진다
                               # (하위 10% 0.51 → 0.64) — 한 방식이 무너질 때
                               # 다른 쪽이 받쳐준다.
BLOB_RADIUS_ALPHA = 0.0        # 0 보다 크면 **반지름만** 이 레벨의 윤곽에서
                               # 다시 잰다 (0 = 끔, 모양 판정 레벨 그대로).
                               # mo + alpha*(mi-mo) 로 계산하므로 작을수록
                               # 배경에 가깝고 콜로니 외곽을 잡는다.
                               # 모양 판정은 검증된 0.5 레벨에 그대로 둔다 —
                               # 낮은 레벨은 이웃·텍스처와 붙어 모양이 망가진다.
BLOB_RADIUS_SCALE = 1.30       # 최종 반지름 보정. max 모드의 중앙값 0.77 에
                               # 곱해 약 1.0 이 되게 맞췄다.
                               #
                               # **정답 박스가 옳고 검출기가 작았다.** 갤러리에서
                               # 눈으로 확인했다 — 초록 정답 박스는 콜로니를
                               # 제대로 감싸는데 검출 원은 일관되게 안쪽에 있다.
                               # 라벨 관행 문제가 아니라 임계값 (mi+mo)/2 가
                               # 콜로니의 반높이 지점을 재기 때문이다.
                               #
                               # 검출 성적에는 영향이 없다(변형 8종 전부 정밀도
                               # 86.4~86.8% / 재현율 64.8~65.2%). 이 값이 중요한
                               # 곳은 로봇 핀 배치·안전 여백과 표시 품질,
                               # 그리고 adaptive_scale 의 크기 추정이다.
                               #
                               # **기각한 대안: BLOB_RADIUS_ALPHA.** 배경에 가까운
                               # 레벨에서 윤곽을 다시 뽑아 외곽을 직접 재는 방식이
                               # 고정 상수보다 원리적이라고 보고 구현했으나, 실측상
                               # 중앙값이 0.90 까지만 가고 꼬리도 더 나빴다
                               # (하위 10% 0.59 대 0.70). "더 작아지면 무시"와 면적
                               # 상한이 낮은 레벨의 윤곽 확장을 막고, 이웃과 붙으면
                               # 상한에 걸려 원래 값으로 돌아가기 때문이다.
BLOB_MIN_RADIUS = 0.0          # 처리 해상도 기준 최소/최대 반지름(px). 0 = 제한 없음.
BLOB_MAX_RADIUS = 0.0
# 콜로니 크기 창 — **접시 지름 대비 비율**. 0 = 제한 없음.
# 비율로 두는 이유: 해상도·카메라·촬영 거리와 무관해진다. petri 접시는 물리적
# 규격품이라 같은 비율이 항상 같은 물리 크기를 뜻한다. 작업 픽셀이나 원본 픽셀을
# 노출하면 이미지마다 의미가 달라져 오퍼레이터가 쓸 수 없다.
# 실측 분포(sample/ 정답 1,886개): 콜로니 지름은 접시 지름의 약 1.2%~45%,
# 중앙값 약 7%. 기본값을 제한 없음으로 두는 이유는 이 분포가 매우 넓어서
# 어떤 고정 창도 진짜 콜로니를 버리기 때문이다 — 크기 제한은 오퍼레이터가
# "이 접시에선 큰 것만" 같은 의도를 표현할 때만 켜는 knob이다.
BLOB_MIN_DIAM_FRAC = 0.0
BLOB_MAX_DIAM_FRAC = 0.0
# LoG 스케일 탐색 범위(처리 해상도 기준 반지름).
#
# **BLOB_WORK_SIZE 와 반드시 함께 움직여야 한다.** detect_blobs 는
# _k = work_size / BLOB_WORK_SIZE 로 이 값을 비례 조정하므로, 기준 상수를 옮기면
# _k 가 1.0 으로 돌아가 물리적 크기 창이 조용히 작아진다. 그러면 콜로니보다 작은
# 한천 텍스처가 검출 대상이 된다.
#
# 실제로 겪었다: work_size 를 1024→1280 으로 올리며 이 값을 안 건드렸더니
# 정밀도가 83.3% → 73.3% 로 무너졌다(검출 1,529 → 1,739, 맞힘은 그대로 1,274).
# 아래 값은 1024 기준 2.5/90.0 에 1.25 를 곱해 그 크기 창을 유지한 것이다.
BLOB_R_MIN = 3.125
BLOB_R_MAX = 112.5
BLOB_N_SCALE = 16              # 스케일 개수(기하급수). 실측 12→F1 65.5,
                               # 16→65.7, 22→65.5 로 16이 최적이나 차이는 미미하다.
BLOB_LOG_THRESH = 0.8          # LoG 후보 문턱 — 낮게 두고 판정은 t 게이트에 맡긴다.
                               # 실측상 0.4/0.8/1.6에서 결과가 **완전히 동일**하다.
                               # t 게이트가 지배하므로 조절 knob으로 가치가 없다.
BLOB_MAX_CANDIDATES = 4000     # 이미지당 후보 상한(NMS 비용 방어)
BLOB_NMS_FRAC = 0.65           # 중심 거리가 max(r)*이 값보다 가까우면 중복.
                               # 실측상 0.5~1.1 전 구간에서 F1 65.7~65.8로 평평하다.
                               # 0.65가 정밀도 동일(90.1%)에 재현율 +0.2%p로
                               # 미세하게 우세해 택했다 — 차이는 노이즈 수준이다.
BLOB_DISH_SHRINK = 0.98        # 접시 반지름 수축 — 테두리 링·메니스커스 배제.
                               # 0.94는 테두리 근처 진짜 콜로니를 버려 재현율을
                               # 0.7%p 깎았고 정밀도 이득은 없었다(실측).
BLOB_DISH_R_MIN_FRAC = 0.28    # 접시 반지름 탐색 범위(이미지 최소변 대비)
BLOB_DISH_R_MAX_FRAC = 0.54
BLOB_PYRAMID_SIGMA = 2.0       # 옥타브 축소 후 유지할 유효 sigma

# 검출 결과 이미지 저장 (로컬 확인용)
OUTPUT_DIR = "output"          # vision/ 기준 상대 경로
DRAW_COLOR = (0, 0, 255)       # BGR 붉은색 (검출 콜로니 표시)
DRAW_PICK_COLOR = (0, 200, 0)  # BGR 초록색 (annotate="pick" 모드에서 피킹 대상 표시)
DRAW_THICKNESS = 2
DRAW_MARKER_PAD = 1.35         # 마커를 콜로니 반지름의 이 배로 그린다.
                               # 딱 맞게 그리면 테두리가 콜로니 경계와 겹쳐
                               # 표시인지 콜로니인지 구분이 안 된다.
MIN_DRAW_RADIUS = 3            # 작은 콜로니도 보이도록 최소 반지름
